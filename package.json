{
  "name": "kirby-boilerplate",
  "version": "1.0.0",
  "description": "Kirby site boilerplate with js css build tools",
  "type": "module",
  "scripts": {
    "dev": "parcel watch src/js/index.js src/css/index.scss --no-cache --no-hmr --dist-dir assets",
    "build": "parcel build src/js/index.js src/css/index.scss --dist-dir assets --no-cache --no-source-maps --dist-dir assets",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "lazysizes": "^5.3.2"
  }
}
